import { Theme } from "theme.slint";

export component MidMain {
    Rectangle {
        background: Theme.palette.background-gradient;

        VerticalLayout {
            padding-bottom: 50px;
        }
    }
}